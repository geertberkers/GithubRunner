name: Oracle Cloud Self-Hosted Runner Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: [self-hosted, linux, oracle]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print hostname
        run: hostname

      - name: Run a test script
        run: echo "Hello from Oracle Cloud self-hosted runner!"